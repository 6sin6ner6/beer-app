import styled from "styled-components";

const Title = styled.h1`
  position: absolute;
  color: black;
  top: 320px;
  left: 50%;
  transform: translate(-50%);
  font-size: 100px;
  font-weight: 200;
`;

export default Title;
